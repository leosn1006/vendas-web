[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -m "not integration and not sefaz"
markers =
    integration: requer banco de dados acessível (excluído da suite padrão)
    sefaz: requer banco + certificado + rede — envia para SEFAZ homologação
